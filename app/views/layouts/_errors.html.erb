<% if object.errors.any? %>
<h3><%= pluralize(object.errors.count, "error") %> prevented this from being submitted.</h2>
<ul>
    <% object.errors.full_messages.each do |message| %>
        <li>
        <%= message %>
        </li>
    <% end %>
</ul>
<% end %>